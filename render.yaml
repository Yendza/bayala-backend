services:
  - type: web
    name: bayala-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
      python manage.py collectstatic --noinput
      python manage.py create_admin  # 👈 Isso cria o superusuário
    startCommand: gunicorn bayala_backend.wsgi:application
